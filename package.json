{
  "name": "kings-invest-backend",
  "version": "1.0.0",
  "description": "Kings Invest Backend",
  "scripts": {
    "build": "cd Backend && npm install --legacy-peer-deps && npm run build",
    "start": "cd Backend && php artisan serve --host=0.0.0.0 --port=$PORT"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 